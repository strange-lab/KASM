
;test code for assembly


MAIN:

L1:
	BTFSS	PORTB, 1 ;LOOP UNTIL PB1 BECOME HIGH
	GOTO L1
	MOVLW	B'00000001' ;SET PB0 LOW
	MOVWF	PORTB
	;CALL DELAY

L2:
	BTFSC	PORTB, 1 ;LOOP UNTIL PB1 BECOME LOW
	GOTO L2
	MOVLW	B'00000000' ;SET PB0 HIGH
	MOVWF	PORTB
	;CALL DELAY

	GOTO	MAIN